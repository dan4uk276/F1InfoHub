spring:
  application.name: F1InfoHub
  datasource:
    url: jdbc:postgresql://localhost:5432/F1InfoHub
    username: postgres
    password: postgres
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect