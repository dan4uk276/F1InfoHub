spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/F1InfoHub
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
youtube:
  api:
    key: ${YOUTUBE_API_KEY}
  scheduler:
    fetch-rate: 24h
    initial-delay: 1s
  video-fetch:
    limit: 10
